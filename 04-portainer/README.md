Execute in your manager1 terminal:
```bash docker-compose.portainer.yaml```

Copy and paste the content of `docker-compose.portainer.yaml` into editor, save and exit.

Copy and execute the content of `portainer.sh` into your manager1 terminal